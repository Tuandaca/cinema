{
  "name": "cinema-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Portofolio-grade Cinema Booking App with AI features",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@10.5.2",
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
